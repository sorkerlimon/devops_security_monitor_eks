apiVersion: v1
kind: Service
metadata:
  name: security-monitor-frontend
  namespace: security-monitor
  labels:
    app: security-monitor-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: security-monitor-frontend
